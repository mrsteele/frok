import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'About Frok', description: 'Turn words into images and still frames into stories. A local creative studio with your library and settings stored on your machine.' };
export default function Page() { return null; }
